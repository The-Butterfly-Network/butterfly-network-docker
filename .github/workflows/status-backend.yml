name: Build Status Backend

on:
  push:
    branches: [main]
    paths:
      - 'status.butterfly-network.win/backend/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Clear Docker builder cache
        run: docker builder prune -af || true

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v6
        with:
          context: ./status.butterfly-network.win/backend
          file: ./status.butterfly-network.win/backend/Dockerfile
          push: true
          tags: clovetwilight3/status-backend:latest
          no-cache: true
